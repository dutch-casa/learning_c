#include <stdio.h>
#include <stdlib.h>

int main() {
    //use [] after the name, and {} to put objects in
    int holyNumbers[] = {1, 3, 7, 12, 40, 555, 777};

    //printf specific index in array
    printf("%d", holyNumbers[1]);
}